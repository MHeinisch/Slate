@model IEnumerable<Slate_Reborn.Models.Shift>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeInfo.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeOfDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WeekDay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeInfo.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeOfDay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeekDay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShiftId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShiftId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShiftId })
        </td>
    </tr>
}

</table>
